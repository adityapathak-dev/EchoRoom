{
  "buildCommand": "cd echoroom && npm install --include=dev && npm run build",
  "outputDirectory": "echoroom/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
